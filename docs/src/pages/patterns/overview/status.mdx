import { navigate } from 'gatsby';
import { data, schema } from "../../../data/components/patterns.js";

<Card shadow="none">
  <Table
    showMenu={false}
    type="resource"
    onRowClick={(props) => {
      navigate(`/patterns/${props.link}`);
    }}
    data={data}
    schema={schema}
    withPagination={false}
  />
</Card>
