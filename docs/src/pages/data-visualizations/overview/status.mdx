import { navigate } from 'gatsby';
import { data, schema } from "../../../data/components/visualizations.js";

<Card shadow="none">
  <Table
    showMenu={false}
    type="resource"
    onRowClick={(props) => {
      navigate(`/data-visualizations/${props.link}`);
    }}
    data={data}
    schema={schema}
    withPagination={false}
  />
</Card>
