import * as React from 'react';
import { Spinner } from '@/index';

// CSF format story
export const all = () => {
  return <Spinner />;
};

export default {
  title: 'Loaders/Spinner/All',
  component: Spinner,
};
