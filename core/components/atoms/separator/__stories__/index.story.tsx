import * as React from 'react';
import Separator from '../Separator';

// CSF format story
export const all = () => (
    <div style={{position: 'relative'}}>
        <Separator />
    </div>
);

export default {
  title: 'Components/Separator/All',
  component: Separator 
};
